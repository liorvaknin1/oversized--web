# OBSIZE — מצב הפרויקט ויומן עבודה

מסמך פנימי (מנוקה ב-strip ב-`deploy.yml`, לא מתפרסם).
**עודכן לאחרונה:** 2026-09-22 · **גרסת נכסים:** `?v=41` · commit `ee72658`

---

## מצב נוכחי

האתר חי ב-**obsize.com** (GitHub Pages מאחורי Cloudflare). מצב **טרום-השקה**: הקטלוג נגלש אבל שום דבר לא ניתן לרכישה.

**עובד ומאומת חי:**
- **עמוד הבית** — header, Hero (תמונת BLOOM full-bleed), מרקיזה, גריד "דרופ חדש", יתרונות, מניפסט, לוקבוק, פוטר.
- **`shop.html`** — קטלוג מלא, גריד 3 עמודות דסקטופ / 2 מובייל (עמוד הבית נשאר עמודה אחת במובייל — showcase מאוצר).
- **PDP** — 6 דפים סטטיים `product-<id>.html` עם OG/canonical/JSON-LD אפויים, גלריה, מדריך מידות, טופס רשימת המתנה.
- **טופס ניוזלטר** (פוטר, index+shop) — מוגן hCaptcha, שולח בפועל, `id="newsletter"` משמש עוגן. ✅ **אומת E2E ידנית (2026-09-24)**: הרשמה עברה והמייל נחת בתיבה הנכנסת.
- ~~טופס הזמנות~~ — **הוסר מהאתר** (אין סליקה). כפתור המגירה מפנה ל"הרשמה לדרופ".
- **טופס רשימת המתנה** (PDP) — מוגן hCaptcha, אומת E2E, שולח גם את המידה והצבע שנבחרו.
- באנר עוגיות + אנליטיקס מגודר-הסכמה, `404.html` ממותג, sitemap, robots.
- **כותרות אבטחה** — מוגדרות ב-**Cloudflare**, לא בריפו (ראה מלכודת #9).

**מצב הקטלוג:**
- 6 מוצרים, כולם `available: false` → באדג' "בקרוב", ללא כפתור הוספה, Schema `PreOrder`.
- **סדר:** BLOOM → Sakur → Paris → Lotus → Eucalyptus → Flamingo (ב-RTL: BLOOM מימין-למעלה).
- **צבע:** שחור בלבד בכל המוצרים.
- **מחירים מוסתרים** בכל מקום (כולל תמונות ה-OG ו-JSON-LD).
- מדריך המידות מכיל את נתוני ה-POM האמיתיים (OBS-TEE-001).

**אנליטיקס:** `GA4_ID` ו-`META_PIXEL_ID` ב-`analytics.js` **ריקים** — כלומר **Cloudflare Web Analytics הוא המדידה היחידה** שרצה. חשוב לזכור לפני שמכבים אותו.

---

## מתגים — איך משנים את הדברים הנפוצים

| מה רוצים | איפה | איך |
|---|---|---|
| להחזיר מוצר למכירה | `products.js` | `available: false` → `true` באותו מוצר. **זה הכל** — הכרטיס, ה-PDP, העגלה וה-Schema נגזרים מזה. (צ'קאאוט יחזור רק כשתהיה סליקה.) |
| להציג מחירים | `products.js` (תחתית) | `window.OBSIZE_SHOW_PRICES = false` → `true`, ולעדכן את ערכי `price`. ⚠️ ראה אזהרת OG למטה. |
| לשנות סדר המוצרים | `products.js` | סדר המפתחות באובייקט = סדר התצוגה. להזיז בלוק. |
| להחזיר צבע למוצר | `products.js` | להוסיף ל-`colors` — **אחרי** שחור, כדי ששחור יישאר ברירת המחדל ויתאים לתמונות. |
| לעדכן מדריך מידות | `product.js` → `SIZE_CHART` | |

**אחרי כל שינוי ב-`products.js` או `product.html`:**
```bash
node scripts/generate-product-pages.mjs
```
(ה-deploy מרגנרט בכל מקרה, אבל עדיף לקמט קבצים מעודכנים.)

**Cache-bust** — להעלות את המספר בכל 8 עמודי המקור ואז לרגנרט:
```bash
perl -pi -e 's/\?v=41\b/?v=42/g' index.html product.html shop.html terms.html privacy.html accessibility.html checkout.html 404.html
node scripts/generate-product-pages.mjs
```

**תחילית `[OBSIZE]` בנושאי המייל — לא לגעת.** כל התראה חדשה חייבת אותה (ראה "מלכודות").

---

## ⚠️ מלכודות שנתפסו — לקרוא לפני עבודה

1. **cache של HTML מסתיר דיפלוי.** לקבצי HTML אין `?v=`, אז הדפדפן מגיש עותק ישן — **שמפנה לגרסאות JS/CSS ישנות**. זה גרם פעמיים לאבחון שגוי של "הדיפלוי נכשל". **תמיד רענון קשיח (Cmd+Shift+R) לפני שמסיקים משהו.** שווה לשקול Cache-Control קצר ל-HTML ב-Cloudflare.

2. **`[hidden]` נדרס ע"י `display` ב-CSS.** `.pdp-add` (דרך `.btn`) ו-`.pdp-section` מגדירים `display`, שגובר על ברירת המחדל של הדפדפן — כפתור "הוסף לסל" נשאר **גלוי ומת**. קיימים spot-fixes (`.pdp-add[hidden]`, `.pdp-section[hidden]`, `.pdp-thumbnails[hidden]`, `.checkout-form-error[hidden]`). **כשמסתירים אלמנט עם `hidden` — לוודא שאין כלל `display` שגובר.**

3. **הגנרטור וזמן-הריצה חייבים להסכים.** `generate-product-pages.mjs` אופה JSON-LD, ו-`product.js` מחשב אותו שוב בדפדפן. פעם אחת הגנרטור צרב `InStock` בזמן שהריצה חישבה `PreOrder` — **זחלן שלא מריץ JS קרא מצב שגוי**. הגנרטור קורא עכשיו את הדגלים מ-`products.js`. **כל שינוי בלוגיקת ה-Schema — לעדכן בשני המקומות.**

4. **נושאי המייל חייבים טוקן משותף.** פילטר הספאם ב-Gmail מסונן על `OBSIZE` בנושא. פעם אחת נושא רשימת ההמתנה היה `רישום לרשימת המתנה — Paris` **בלי שום טוקן מותג** — הפילטר תפס הזמנות ופספס בשקט כל רישום, ונראה כאילו הוא עובד. **כל נושא חדש מתחיל ב-`[OBSIZE]`.**

5. **תמונות ה-OG מכילות טקסט צרוב.** ב-6 `og-*.png` המחיר "ILS 169" היה **בתוך הפיקסלים** — שיתוף קישור הציג מחיר שלא קיים. הוסר בצביעה מעל הפאנל השחור. ⚠️ **אם מחליפים את התמונות — המחיר עלול לחזור.** לבדוק לפני העלאה.

6. **zsh לא מפצל משתנה לא-מצוטט.** `FILES="a.html b.html"; cmd $FILES` נכשל (בניגוד ל-bash). להעביר קבצים מפורשות.

7. **פרטי אשראי לא נשלחים — בכוונה.** `checkout.js` בונה payload JSON **ידני** כדי להשמיט אותם. **אסור** לעבור ל-`new FormData(form)` — זה היה סוחב את שדות הכרטיס ל-Web3Forms. (הקובץ כרגע מוסר מהאתר, אבל הכלל תקף לכשיחזור.)

8. **טענות שיווקיות דורשות גיבוי.** סבב 2026-09-22 הסיר עשרות טענות לא מגובות (משלוח לכל העולם, תשלום מאובטח ללא סליקה, "שלא נסדק", "מחזיק שנים", הנחת 10%). **לפני שמוסיפים טענה — לוודא שהיא נכונה בפועל.**

9. **כותרות האבטחה לא נמצאות בריפו — הן ב-Cloudflare.** חמש כותרות חוזרות מכל עמוד, וכולן מוגדרות ב-Transform Rule בשם **"Security headers"** (Rules → Transform Rules → Modify Response Header):

   ```
   Content-Security-Policy:   frame-ancestors 'none'
   X-Frame-Options:           DENY
   Permissions-Policy:        geolocation=(), microphone=(), camera=()
   Strict-Transport-Security: max-age=15552000
   X-Content-Type-Options:    nosniff
   ```

   **למה לא בריפו**: GitHub Pages **לא תומך בכותרות מותאמות** — אין `_headers`, אין `.htaccess`. ו-`frame-ancestors` ו-`X-Frame-Options` **מתעלמים מהם לחלוטין** כשהם מגיעים ב-`<meta http-equiv>` (מפורש במפרט ה-CSP). כלומר הוספה שלהם לתגי ה-meta הייתה **נראית כמו הגנה ולא עושה כלום**.

   🚨 **אסור להכניס CSP מלא ל-Transform Rule הזה.** הערך חייב להישאר `frame-ancestors 'none'` בלבד. כש-header CSP ו-meta CSP מגיעים יחד הדפדפן אוכף את **שתיהן** בחיתוך המחמיר — CSP מלא בכותרת ייחתך מול זה שב-meta ויחסום את hCaptcha, את Web3Forms, את הגופנים ואת ה-beacon. **שינוי CSP של האתר נעשה בתגי ה-meta ב-14 קבצי ה-HTML, לא כאן.**

---

## פתוח / לא הושלם

**אין חסמים אדומים.** כל מה שנותר הוא דרגה שנייה:

- **שגיאת CSP אחת בקונסול** — inline של Cloudflare JS Detections. **הוכרעה כ"משאירים"** (ראה החלטות). לחזור רק אם התוכנית תשודרג ל-Pro.
- **גיבוי להתראות** — לא לסמוך רק על מייל. הדשבורד של Web3Forms שומר כל פנייה; שווה להיכנס מדי פעם, או להוסיף ערוץ שני.
- **קטע ביקורות** — הוסר ברדיזיין, לשקול החזרה.
- **אין אינטגרציית סליקה** — `checkout.html` + `checkout.js` **הוסרו מהאתר** (strip ב-deploy.yml) ונשארו ב-git. כשתהיה סליקה: להסיר משורת ה-strip ולהחזיר את כפתור המגירה ל-checkout.

---

## החלטות והנימוקים (לא ללעוס מחדש)

- **"בקרוב" ולא "sold out"** — *sold out* לא נכון עובדתית (לא נמכר כלום), משדר "פספסת", ולא מחזיר מידע. *בקרוב* + רשימת המתנה מדויק, צופה קדימה, **ומספר איזה מוצר ומידה מבוקשים לפני שקונים מלאי**.
- **`PreOrder` ולא `OutOfStock`** ב-Schema — `OutOfStock` על כל הקטלוג לאורך זמן מסמן לגוגל קטלוג מת.
- **מחיר מוסתר ולא מחיר placeholder** — פרסום מחיר שלא נקבע הוא price mismatch מול המחיר העתידי, וגוגל מעניש על זה.
- **JS Detections נשאר דלוק** — בתוכנית **Free** אין טוגל נפרד; המתג היחיד הוא כיבוי **Bot Fight Mode כולו**. המצב היום = BFM עובד + JSD חסום (הגנה חלקית); כיבוי = הגנה אפסית, באתר עם טופס הזמנות וטופס לידים. קונסול נקי לא שווה את זה. **ב-Pro+ קיים טוגל נפרד** — לחזור לזה רק אז.
- **Cloudflare Analytics אושר ב-CSP ולא כובה** — הוא המדידה היחידה שרצה (GA4/Meta ריקים).
- **`soldOut` פר-מידה מבוטל במצב "בקרוב"** — "בקרוב, אבל XXL אזל" זו סתירה; כל המידות נבחרות כדי שרשימת ההמתנה תתפוס ביקוש אמיתי.
- **עגלה ו-checkout לא נגעו בסבב הסתרת המחירים** — אף מוצר לא רכיש, המסלולים לא רצים; יהיו נכונים כשיהיו מחירים.
- **עברית תמיד** בתשובות.

---

## יומן סבבים

**2026-06-21/22 · רדיזיין עמוד הבית + Shop** (`4380185`, `b623e6a`, v=30)
Hero full-bleed, לוקבוק עם צילומי דוגמן, גריד עם hover crossfade, PDP עם גלריה, WebP+JPG לכל הנכסים. נבנה `shop.html`. נוקו ~473 שורות CSS מת + בלוק parallax מת ב-JS + 8 קבצי נכסים (~1.5MB). אוחדו שתי קונבנציות גרסה (v=29/v=16) ל-v=30.

**2026-07-04 · אבטחה + שיפורים** (v=31)
`reviseAgainstCatalog` — מחיר/שם נגזרים מהקטלוג, localStorage לא נסמך (אומת: עגלה מזויפת עם מחיר 1 ו-qty 500 → סכום נכון). הקשחת `loadCart`. הגנת `hasOwnProperty` (`?id=__proto__`). ולידציית תוקף כרטיס. **דפי מוצר סטטיים ל-OG** + `generate-product-pages.mjs`. מודאל מדריך מידות. `404.html`. הוסרו כפתורי חיפוש/חשבון מתים.

**2026-07-11 · hCaptcha ל-checkout** (`b2b4b6b`, v=32)
Captcha Protection הופעל בדשבורד Web3Forms → כל שליחה בלי טוקן נדחית. הותאם ל-payload הידני (לא FormData — כדי לא לשלוח כרטיס). **בוטל fire-and-forget**: מסך התודה מותנה ב-`success:true`. ניוזלטר נוטרל. **E2E אומת ע"י המשתמש**: הזמנה → תודה → מייל התקבל.

**2026-08-16/17 · Pre-launch "בקרוב"** (`b9e97cd` v=33, `847930b` v=34)
`available: false` לכל 6. באדג' "בקרוב", ללא quick-add, PDP עם טופס רשימת המתנה. עגלות ישנות מסוננות בשתי שכבות → אי אפשר להזמין. נתפס באג `[hidden]`. תוקנו סתירות טקסט: תגית "NEW DROP" מעל "לא במלאי" → "בקרוב"; כרטיסי related קיבלו סימון.

**2026-08-17 · תמונת BLOOM, צבע, מידות, מחירים** (`fe2a13c`, `f1fd3a0` v=35, `27d5c19` v=36, `f28aa53` v=37)
הוחלף `bloom-look` שנשא הדפס "DIVISION" מיושן. הוסר הצבע הלבן מ-BLOOM (היה **ברירת המחדל** בזמן שהתמונות שחורות). הוזנו נתוני POM אמיתיים — הערכים הקודמים היו שגויים מהותית (**כתפיים 54→58**, קריטי ב-drop-shoulder). הוסתרו מחירים בכל מקום כולל צריבה בתמונות OG.

**2026-08-23 · סדר קטלוג** (`74c1297`, v=38)
BLOOM ראשון, Sakur אחריו. נגרר גם ל-"אולי תאהבו גם".

**2026-09-13/15 · CSP, AGENTS.md, דיוור** (`022ca13` v=39, `dbe0bc4`, `c12d78d` v=40)
beacon של Cloudflare Analytics אושר ב-CSP ב-14 קבצים (אומת: נטען **וגם** ה-POST ל-`/cdn-cgi/rum` עבר). `AGENTS.md` הפך ל-symlink ל-`CLAUDE.md` (היה העתק זהה שהיה מתפצל) ונוסף ל-strip. **דיוור נפתר**: פילטר Gmail + תחילית `[OBSIZE]` בנושאים — אומת שהמיילים נוחתים בתיבה הנכנסת.

**2026-09-22 · סבב ניקוי טקסטים + הסרת צ'קאאוט** (v=41)
תיקון רוחבי של טענות לא מדויקות לפי רשימה: משקל בד 250→**240 גרם**; חומר → **"100% כותנה מסורקת ring-spun"**; גזרה → **drop shoulder** ב-Sakur/Lotus; הוסרו "שלא נסדק" (×4), "עשרות כביסות", "מחזיק שנים" (×2), "אריזת מתנה" (×2), הנחת 10%, ו"המידות מתאימות לגוף ישראלי".
**משלוח**: ישראל בלבד (לא "לכל העולם"), **חינם מעל 299 ₪** — ב-PDP, באקורדיון וב-terms; "משלוח חינם" במטא → "משלוח מהיר"; הוסרה כפילות 14/30 בתקנון (כעת 30 יום).
**הסרת צ'קאאוט**: `checkout.html` + `checkout.js` נוספו לשורת ה-strip (נשארים ב-git); בלוק "תשלום מאובטח" הוסר מעמוד הבית; terms/privacy נוסחו מחדש שאין רכישה מקוונת; 9 כפתורי מגירה → **"הרשמה לדרופ"** שמפנה לעוגן `#newsletter`.
**הניוזלטר הוחייה** (היה מנוטרל): widget hCaptcha משלו, שליחה עם `await` + `success:true`, נושא `[OBSIZE]`, CSP הורחב ב-index/shop. ✅ **אומת E2E ע"י המשתמש (2026-09-24)** על האתר החי — הרשמה עברה והמייל הגיע לתיבה הנכנסת, כלומר גם תחילית ה-`[OBSIZE]` והפילטר עובדים עליו. ה-`<form>` הפך למעטפת כדי שה-widget לא ישבור את שורת ה-flex.
**נגישות**: "בהתאם לתקן" → "בשאיפה לעמוד בדרישות"; "ונבדקת מעת לעת" הוסר.
סטיות שנמצאו מול הרשימה: "שלא נסדק" היה **4 מופעים ולא 5** (Paris פעם אחת), ויש **6 דפי product-* ולא 7**.

**2026-09-24 · הגנת clickjacking** (ללא שינוי קוד)
`X-Frame-Options: DENY` כבר היה פעיל (אומת שמקורו ב-Cloudflare — GitHub Pages לא שולח אותו). נוסף `Content-Security-Policy: frame-ancestors 'none'` ל-Transform Rule "Security headers". **לא נגעתי בקוד** — ראה מלכודת #9 למה אי אפשר.
אומת בכל 6 העמודים: header CSP **יחיד** עם הערך הנכון, XFO לא נדרס, אפס שגיאות CSP חדשות, ו-hCaptcha נטען בשני הטפסים (iframe 302x76, שדה הטוקן בתוך הטופס). נבדק גם שה-beacon וגופן Heebo לא נפגעו.
