# OBSIZE — סיכום סשן (2026-06-21)

עיצוב מחדש של עמוד הבית (Phase 1) — מוזג ל-`main` ודופלוי לאתר החי (obsize.com).

## מה עשינו
- **Hero**: הומר לתמונת רקע מלאה (full-bleed) של BLOOM שמכסה את כל הקטע, עם טקסט מוטל משמאל מעל הקיר הכהה, scrim להבטחת קריאוּת, ו-`object-position` מכוון. במובייל הטקסט מעוגן למעלה כדי שה-CTAs יהיו מעל באנר העוגיות. בהמשך הוחלפה תמונת ה-Hero לגרסה חדה יותר.
- **לוקבוק**: 4 ריבועי placeholder הוחלפו בצילומי דוגמן אמיתיים של 4 מוצרים שונים — Lotus / Flamingo / BLOOM / Sakur (מגוון פוזות/תפאורות), עם זום עדין ב-hover.
- **גריד NEW DROP**: התמונה הראשית בכרטיס היא צילום המוצר על רקע שחור; צילום הדוגמן מתחלף ב-hover (crossfade). שיפורי עיצוב (מחיר בהיר יותר), הסרת קישור "כל הפריטים" מיותר, ותיקון באג השהיית transition.
- **עמוד מוצר (PDP)**: נוספה גלריית תמונות — תמונה ראשית + thumbnails להחלפה (שחור + דוגמן).
- **תמונות**: כל הנכסים מוטבו ל-WebP + JPG תחת `assets/images/` (hero / lookbook / products).
- **QA מובייל**: תוקן כפתור CTA בלתי-נראה בתפריט (לבן על לבן); הועלה z-index של מגירת העגלה מעל באנר העוגיות כך שכפתור ה-checkout גלוי ולחיץ.
- **ניקוי לפני מיזוג**: נמחקו 8 קבצי נכסים לא בשימוש (~1.5MB) ו-~473 שורות CSS מת (Hero ישן, testimonials/why/final-CTA, placeholders); תוקנו קישורי ניווט שבורים ב-`product.html`.
- **מיזוג ודיפלוי**: סקירה מלאה מול `main`, מיזוג `--no-ff` (commit `4380185`), push, ו-workflow "Deploy to GitHub Pages" הסתיים בהצלחה. הענף `redesign/homepage` נמחק (local + remote).

## החלטות מרכזיות
- **עברית תמיד** בתשובות (נשמר ל-memory).
- **גריד מוצרים נשאר עמודה אחת במובייל** — showcase פרמיום מאוצר; 2 עמודות יישמרו לעמוד ה-Shop העתידי.
- **תמונה ראשית בכרטיס = צילום מוצר על שחור**, דוגמן ב-hover (מתאים לאתר הכהה).
- **סדר לוקבוק**: Lotus / Flamingo / BLOOM / Sakur.
- **ניוזלטר נדחה** — הטופס נשאר בפוטר, וה-CSP מאפשר `api.web3forms.com`, אך לא הופעל/נבדק.
- **לא נגענו ב-checkout/תשלום** (דמו בלבד).
- ניקוי הבלוק הרספונסיבי המעורב (styles.css ~951–999) וקוד ה-parallax הישן ב-JS — **נדחו** (no-op לא מזיק; סבב נפרד בהמשך).

## מצב נוכחי
**עובד (חי ב-obsize.com):**
- כל קטעי עמוד הבית: header, Hero, מרקיזה, NEW DROP, יתרונות, מניפסט/דרופים, לוקבוק, פוטר.
- עגלה (drawer + הוספה מהירה), PDP עם גלריה, תפריט מובייל, באנר עוגיות + אנליטיקס מגודר.
- אומת: אין גלישה אופקית, אין שגיאות קונסול, רספונסיבי ב-1440/768/375.
- דיפלוי הצליח; ה-HTML מהשרת מגיש `styles.css?v=29` + `hero-wide.webp`.

**פתוח / לא הושלם:**
- ניוזלטר לא מחווט (טופס קיים, CSP מוכן).
- עמוד Shop/קטלוג מלא (2 עמודות במובייל) — לא נבנה. ← הבא בתור.
- ~~CSS מת בבלוק הרספונסיבי~~ ✅ נוקה (2026-06-22).
- ~~parallax ישן ב-`script.js`~~ ✅ הוסר (2026-06-22).
- אין קטע ביקורות (הוסר; קישור "ביקורות" הוסר מ-product.html).

## צעדים הבאים (לפי עדיפות)
1. ✅ **בוצע (2026-06-22)** — כפתור ה-+ (הוספה מהירה) נבדק במגע אמיתי ועובד.
2. **החלטה על הניוזלטר** (נדחה): לחווט ל-Web3Forms (success/error graceful) **או** להסיר את הטופס ואת `api.web3forms.com` מה-CSP.
3. ✅ **בוצע (2026-06-22)** — נבנה `shop.html`: עמוד קטלוג מלא (6 מוצרים), גריד `.shop-grid` ייעודי **3 עמודות בדסקטופ / 2 במובייל** (בניגוד ל-showcase של עמוד הבית שנשאר עמודה אחת). שימוש חוזר ב-`#productsGrid` + `products.js` + `script.js` (אותו renderer/עגלה/ניווט). ללא פילטרים (גרסה 1). קישורי "שופ"/"כל המוצרים" ב-`index.html` ו-`product.html` ("קולקציה") עודכנו לכוון ל-`shop.html`; הניווט הפנימי בעמוד החנות מצביע חזרה ל-`index.html#...`. אומת בדפדפן: 6 כרטיסים, 3/2 עמודות, אין שגיאות קונסול, quick-add פותח עגלה ומוסיף פריט תקין.
4. ✅ **בוצע (2026-06-22)** — סבב ניקוי JS: הוסר בלוק ה-Hero Parallax המת מ-`script.js` (mouse-move על `[data-parallax-speed]` שלא קיים + scroll-drift שיורי). אומת בדפדפן: אין שגיאות קונסול, hero תקין ב-desktop+mobile.
5. ✅ **בוצע (2026-06-22)** — ניקוי CSS מת בבלוק הרספונסיבי הישן: הוסרו חוקי `why-grid/why-item`, `testimonials-grid/testimonial-card`, `hero-floating-tag`, `cta-final-title`, `hero-visual margin-top` (no-op), ובלוק `@media 480px` הישן (מוחלף ע"י `#hero .hero-btns` ב-~2198). מספרי השורות ב-notes הישן (951–999) היו שגויים — האזור האמיתי היה ~496–542.
6. **לשקול קטע ביקורות/המלצות** (נדחה) אם רוצים להחזיר (הוסר ברדיזיין).

### נותר dead-code קטן (לא קריטי)
- ~~`index.html:111` — `data-img`~~ ✅ הוסר (2026-07-04).

## סבב אבטחה + שיפורים (2026-07-04)
- **אבטחה — מחירים מהקטלוג ב-checkout**: `checkout.js` כבר לא סומך על localStorage — שם/מחיר/תמונה נגזרים מחדש מ-`products.js` לפי id (`reviseAgainstCatalog`); פריטים שלא בקטלוג מושמטים; qty נחתך ל-1..99. אומת: עגלה מזויפת (מחיר 1, qty 500, פריט פיקטיבי) → סכום נכון ₪16,731. `products.js` נוסף ל-`checkout.html`.
- **הקשחת `loadCart`** (script.js + checkout.js): אימות מערך, קירוץ מספרים, סינון פריטים חסרי id/name — localStorage משובש לא מפיל יותר את העגלה/צ'קאאוט.
- **`product.js`**: הגנת `hasOwnProperty` (`?id=__proto__` → "מוצר לא נמצא" במקום קריסה).
- **ולידציית תוקף כרטיס בעבר** ("הכרטיס פג תוקף").
- **דפי מוצר סטטיים ל-OG**: `scripts/generate-product-pages.mjs` מג'נרט `product-<id>.html` (6 דפים, קומיט + רגנרוט אוטומטי ב-deploy.yml) עם OG/canonical/JSON-LD אפויים — שיתוף בוואטסאפ/פייסבוק מציג את המוצר הנכון. `product.js` קורא id גם מ-`body[data-product-id]`; קישורי כרטיסים/related/sitemap עודכנו. **כשמשנים את products.js או product.html — להריץ `node scripts/generate-product-pages.mjs`** (ה-deploy מרגנרט בכל מקרה).
- **מודאל מדריך מידות** במקום alert (טבלה S–XXL, Esc/backdrop, נגיש). ⚠️ המידות סטנדרטיות — **לאמת מול מדידות אמיתיות** ולעדכן ב-`product.js` (SIZE_CHART).
- **404.html ממותג** (noindex); **הוסרו כפתורי חיפוש/חשבון** המתים מהנאב (index+shop); `shop.html` נוסף ל-sitemap; כותרת PDP אוחדה ל-`<שם> — OBSIZE`.
- **bump ל-`?v=31`** בכל העמודים.
- **פעולות ידניות שנותרו (של המשתמש):** (1) לשקול Cloudflare Transform Rule ל-`frame-ancestors`/`X-Frame-Options` (clickjacking); (2) למסור מידות אמיתיות למדריך המידות.

## סבב hCaptcha ל-checkout (2026-07-11, commit `b2b4b6b`, v=32)
- **הקשר**: המשתמש הפעיל בדשבורד Web3Forms — Captcha Protection = hCaptcha + Spam Level = Strict. מרגע זה כל שליחה בלי טוקן נדחית.
- **אבחון קריטי**: ה-checkout שולח `fetch()` עם payload JSON **ידני** (בכוונה — כדי **לא** לשלוח פרטי כרטיס). לכן הפתרון הסטנדרטי (`new FormData(form)`) **אסור** כאן — היה סוחב את שדות האשראי ל-Web3Forms.
- **checkout.html**: widget `.h-captcha[data-captcha]` מעל כפתור השליחה + `web3forms.com/client/script.js`; **CSP הורחב** ל-`hcaptcha.com`+`*.hcaptcha.com` (script/style/frame/connect) ו-`web3forms.com` (script).
- **checkout.js**: קורא `h-captcha-response`, מזריק ל-payload הידני (כרטיס עדיין לא נשלח); חוסם שליחה בלי טוקן (הודעה); עבר ל-`async/await` — **success מוצג רק כש-`data.success===true`**. בכשל: עגלה נשמרת, captcha מתאפס, שגיאה מוצגת. **בוטל ה-fire-and-forget** — אין יותר כשל שקט.
- **ניוזלטר (index+shop)**: חולק את אותו מפתח → ה-POST החי **נוטרל** (ולידציה+תודה בלבד). `api.web3forms.com` הוסר מ-connect-src ב-index/shop (checkout שומר עליו). להפעלה עתידית: מפתח נפרד בלי captcha, או widget משלו.
- **אומת ב-preview**: widget נטען (iframe, `window.hcaptcha`), אין שגיאות CSP, שליחה בלי טוקן נחסמת (עגלה נשמרת), אין גלישה במובייל. **אומת חי**: widget+client script+CSP frame-src ב-checkout.html, token+success-gate ב-checkout.js, 0 קריאות web3forms ב-script.js.
## סבב Pre-launch — כל המוצרים "בקרוב" (2026-08-16, commit `b9e97cd`, v=33)
- **החלטה**: "בקרוב" ולא "sold out". *sold out* לא נכון עובדתית (לא נמכר כלום), משדר "פספסת", ולא מחזיר מידע. *בקרוב* + רשימת המתנה = מדויק, צופה קדימה, **ומספר איזה מוצר/מידה באמת מבוקשים לפני שקונים מלאי**.
- **`products.js` — `available: false` על כל 6 המוצרים. זהו מקור האמת היחיד**: כשמגיעה סחורה, משנים ל-`true` ותו לא. שום קובץ אחר לא דורש עריכה.
- **כרטיסים** (`script.js`): באדג' "בקרוב", כפתור ה-+ **מוסר לגמרי** (לא disabled — שלא יהיה מה ללחוץ שלא עובד).
- **PDP** (`product.js` + `product.html`): מסתיר כמות + "הוסף לסל", מציג טופס רשימת המתנה שנשלח ל-Web3Forms **עם המידה והצבע שנבחרו**. `await` + בדיקת `success:true` (אין כשל שקט). `soldOut` פר-מידה **מבוטל** במצב בקרוב (חסר משמעות לפני השקה, וסותר את "בקרוב"). Schema → **PreOrder** (לא OutOfStock, שמסמן לגוגל קטלוג מת).
- **עגלות ישנות** — הקצה המסוכן: `loadCart` (script.js) מסנן פריטים לא-זמינים, ו-`reviseAgainstCatalog` (checkout.js) מפיל אותם. אומת: עגלה עם 2 פריטים ב-localStorage → מסך "העגלה ריקה", אי אפשר לשלוח הזמנה.
- **באג שנתפס**: `.pdp-add` (דרך `.btn`) ו-`.pdp-section` מגדירים `display`, שדורס את `[hidden]` של הדפדפן — כפתור "הוסף לסל" נשאר גלוי ומת. תוקן ב-`styles.css`. **דפוס לשים לב אליו בקוד הזה** — כבר היו spot-fixes דומים (`.pdp-thumbnails[hidden]`, `.checkout-form-error[hidden]`).
- **CSP של `product.html` הורחב** ל-hcaptcha/web3forms (כמו checkout), כי טופס הלידים דורש captcha על אותו מפתח.
- ✅ **בדיקת E2E של טופס רשימת ההמתנה עברה (2026-08-17)**: הרישום נשלח, ה-API אישר, **והמייל מגיע** ל-lior@obsize.com. הצינור עובד מקצה לקצה. ⚠️ אבל המיילים **נוחתים בתיקיית הספאם** — ראה סעיף הדיוור בחסמים הפתוחים.
- ✅ **סבב תיקון המשך (commit `847930b`, v=34)** — נמצאו שתי סתירות טקסט: (1) תגית ה-PDP הציגה "NEW DROP" מעל "הפריט עוד לא במלאי" → כעת "בקרוב"; (2) כרטיסי "אולי תאהבו גם" נראו כניתנים לקנייה → קיבלו סימון "בקרוב". Paris היה הבולט כי הוא המוצר הראשון בקטלוג.
- ⚠️ **סיבת השורש ל"לא התעדכן"**: **ה-HTML עצמו נשמר ב-cache של הדפדפן**. לקבצי ה-HTML אין `?v=`, אז דפדפן עם עותק ישן טוען אותו — והוא מפנה לגרסאות ה-JS/CSS *הישנות*. ה-bump עוזר רק אחרי שה-HTML החדש נטען. **בכל בדיקה אחרי דיפלוי: רענון קשיח (Cmd+Shift+R)**, אחרת רואים מצב ישן ונדמה שהדיפלוי נכשל. שקול Cache-Control קצר ל-HTML ב-Cloudflare.

- ✅ **בדיקת E2E אמיתית עברה (2026-07-11, ע"י המשתמש על האתר החי)**: הזמנה עם captcha פתור → מסך "תודה על ההזמנה" → **מייל התקבל ב-lior@obsize.com**. שליחה בלי captcha נחסמת. הזרימה מוכחת מקצה-לקצה: "תודה" מותנה ב-`success:true`, כך שאין יותר כשל שקט. הנמען המוגדר בדשבורד = lior@obsize.com (אושר).

## הסרת הצבע הלבן מ-BLOOM (2026-08-17, commit `f1fd3a0`, v=35)
- BLOOM היה המוצר היחיד עם שני צבעים, והלבן לא נדרש לדרופ הזה.
- **באג שנתפס אגב כך**: `'לבן'` היה **ראשון** ב-`colors`, כלומר ברירת המחדל — ה-PDP נטען עם "צבע: לבן" בזמן ששתי תמונות הגלריה מציגות חולצה **שחורה**, והכרטיסים נשאו `data-product-color="לבן"`.
- כעת כל 6 המוצרים: **שחור בלבד**. אומת חי: 0 מופעים של "לבן" בקטלוג, עיגול צבע יחיד בכל כרטיס וב-PDP.
- 💡 להחזרת הלבן: להוסיף שורה ל-`colors` של bloom — **אחרי** שחור, כדי שהשחור יישאר ברירת המחדל ויתאים לתמונות.

## בעיות / חסמים פתוחים

### 🔴 דיוור: מיילי Web3Forms נוחתים בספאם (פתוח, 2026-08-17)
- **הסימפטום**: גם מיילי ההזמנות וגם רשימת ההמתנה מגיעים — אבל לתיקיית הספאם.
- **הסיבה**: Web3Forms שולח מהשרתים שלהם. ל-`obsize.com` **אין SPF/DKIM** שמאשרים להם לשלוח בשמו, אז ספק המייל רואה הודעה שמתחזה ל-OBSIZE ממקור לא מאומת. **שום שינוי בקוד לא יתקן את זה** — זה אימות דומיין.
- **⚠️ חומרה עסקית**: הזמנה אמיתית עלולה להתפספס. השקענו סבב שלם על "אין כשל שקט בקוד" — וזה בדיוק כשל שקט, שלב אחד אחרי הקוד.
- **פתרונות לפי סדר**: (1) **כלל/פילטר בתיבה** — "מכיל `OBSIZE Orders` או `OBSIZE Waitlist` → אף פעם לא ספאם" (סימון חד-פעמי לא תמיד מחזיק); (2) **לא לסמוך רק על מייל להזמנות** — הדשבורד של Web3Forms שומר הכל, או להוסיף ערוץ שני (Webhook/Slack/טלגרם); (3) **הפתרון האמיתי** — SPF/DKIM ב-DNS + שירות דיוור טרנזקציוני או SMTP מותאם. עבודת DNS/Cloudflare, לא נגעתי.

### 🟡 שגיאות CSP בקונסול — מהזרקות Cloudflare (פתוח, אבחון מלא בוצע)
שתי שגיאות בקונסול, **שתיהן לא מהקוד שלנו** ולא שוברות שום דבר:
1. **`static.cloudflareinsights.com/beacon.min.js`** נחסם — Cloudflare **Web Analytics**. host קבוע, אז *ניתן* לתקן ב-CSP (`script-src` + `connect-src`) — או פשוט לכבות בדשבורד (ממילא כפול ל-GA4+Meta Pixel שכבר רצים בהסכמה).
2. **סקריפט inline של `__CF$cv$params`** נחסם — Cloudflare **JS Detections** (Bot Fight Mode). **hash לא יעבוד**: אומת בפועל שה-hash משתנה בכל טעינה (`sha256-8ISE…`, `sha256-RDD8…`, `sha256-ZMA0…`, `sha256-nxHS…`) כי ה-tokens `r`/`t` מתחלפים. **חייב כיבוי בדשבורד Cloudflare**, אלא אם עוברים ל-CSP ב-response header עם nonce (שינוי ארכיטקטוני).
- **ממצא מפתח**: בריפו שלנו יש **אפס** סקריפטים inline רצים ו**אפס** `onclick=` וכו'. כל בלוקי ה-`<script>` הפנימיים הם `application/ld+json` (דאטה — CSP לא חוסם). כלומר "להעביר inline לקובץ חיצוני" **לא ישים כאן** — המקור הוא Cloudflare.
- לפי הכללים לא נגעתי בהגדרות Cloudflare. אם תבחר במסלול allowlist ל-beacon — אני יכול לעדכן CSP ב-13 העמודים.

### שאר הפתוחים
- **מדריך מידות**: המידות ב-`SIZE_CHART` (product.js) סטנדרטיות — לאמת מול מדידות אמיתיות.
- **ניוזלטר**: מנוטרל (חולק מפתח חסום-captcha). להפעלה: מפתח נפרד בלי captcha, או widget משלו.
- **clickjacking**: לשקול Cloudflare Transform Rule ל-`X-Frame-Options`/`frame-ancestors`.
- **cache של HTML**: לקבצי HTML אין `?v=` → דפדפן מגיש עותק ישן שמפנה ל-JS ישן. **תמיד רענון קשיח אחרי דיפלוי.** שקול Cache-Control קצר ל-HTML.
- ✅ **deploy בוצע (2026-06-22, commit `b623e6a`)**: כל נכסי ה-cache-bust אוחדו ל-`?v=30` בכל 7 העמודים (היו שתי קונבנציות — v=29 בעמודים המעוצבים, v=16 בעמודי מדיניות/checkout). push ל-main, ה-workflow הסתיים. אומת מול האתר החי: `shop.html`=200, `styles.css?v=30`=200 (מכיל `.shop-grid`), `script.js?v=30`=200 (ללא parallax), קישורי "שופ"→`shop.html`.
- `shop.html` הוא עמוד מתפרסם (לא dev) — לא צריך strip ב-deploy.yml.
- **Cloudflare edge cache**: ייתכן שמשתמשי-קצה יראו עיצוב ישן זמנית עד שה-cache יתרענן (רענון קשיח פותר). ה-origin כבר מעודכן.
- תזכורת אבטחה: ה-CSP הורחב ל-origin אחד (`api.web3forms.com`, ב-connect-src בלבד) — אם הניוזלטר לא יופעל, כדאי להסיר.
